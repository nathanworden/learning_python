# dynamically typed - a model that keeps track of types for you automatically instead of requring declaration code.

# strongly typed - a constraint that means you can perform on an object only operations that are valid for its type.

# (can be both)